# Daily Brief — Backlog

## Open

- [ ] **Fix 10 — Stale article re-ingestion**
  Fetcher is surfacing old cached articles (SemiAnalysis URLs from Sept 2025 appearing in March 2026 briefs). Investigate deduplication logic and add a published-date cutoff — articles older than 7 days should not be re-ingested or re-surfaced in synthesis.

- [ ] **Fix 11 — Graphical header with tier counts + theme callout**
  Replace current plain dark header in `send_brief.py` with new design. Four tier count tiles (Act Now / Monitor / Background / Flags) with colored top borders (red/amber/slate/purple). Theme callout strip below with glowing blue dot and dominant narrative line. IBM Plex Mono + IBM Plex Sans via Google Fonts import. All inline CSS for Gmail compatibility. Tier counts pulled from actual brief metadata. Theme line generated by synthesis prompt as a single sentence added to brief output. Reference mockup: `brief_header_mockup.html`.

- [ ] **Fix 12 — Add new sources**
  Add the following to `config/feeds.yaml` and `sources.md`:
  - **Packet Pushers** — Heavy Networking + Network Break feeds. Category: networking. Trust: medium. Flag episodes with sponsor mentions in title/description as vendor trust.
  - **Light Reading** — Category: networking / optical. Trust: medium.
  - **The Next Platform** — Category: semiconductors / AI infrastructure. Trust: medium.
  - **Stacey Higginbotham / IEEE Spectrum** — Category: networking / infrastructure. Trust: high.
  - **Calculated Risk (Bill McBride)** — Category: finance / macro. Trust: high.
  - **Net Interest (Marc Rubinstein)** — Category: finance. Trust: medium.

- [ ] **Fix 13 — Cap arxiv items in Flags**
  Pre-Publication Research section in Flags is too noisy. Cap at 2–3 most relevant items per brief. Select by relevance to networking, AI infrastructure, or finance. Discard the rest silently.

- [ ] **Fix 14 — README maintenance**
  Claude Code should update `README.md` to reflect current codebase state after any significant set of changes. Add this as a standing instruction at the end of each BACKLOG.md work session prompt.

- [ ] **Fix 15 — Optimize token usage**
  - Truncate article text to 300–400 chars per article before sending to synthesis
  - Verify `--max-stories` cap is enforced before API call (80 morning / 40 midday)
  - Pre-filter zero-signal items at normalize stage: zeitgeist/Reddit entries and uncorroborated vendor-only items below a minimum threshold should not be sent to Claude
  - Verify story grouping deduplication is working — one grouped block per event, not N individual articles
  - Review and tighten `morning_system.md` and `midday_system.md` — remove verbose instructional prose without losing fidelity
  - Check actual token usage in `logs/synthesize.log` before optimizing blind

---

## Done

- [x] **Fix 1 — Brief type in subject line** — `--brief-type` passed from cron → main.py → send_brief.py → email subject
- [x] **Fix 2 — Cron timezone** — `CRON_TZ=America/Los_Angeles` set; morning 06:00, midday 11:30 PDT
- [x] **Fix 3 — Add "Why it matters" to Tier 2 items** — added to synthesis prompt
- [x] **Fix 4 — SEC Form 4 suppressed** — bare stubs suppressed until EDGAR enrichment is implemented
- [x] **Fix 5 — Cap Tier 3 at 5 items** — enforced in synthesis prompt
- [x] **Fix 6 — Single-source geopolitical stories default to Tier 2** — enforced in scoring rules
- [x] **Fix 7 — Jarvis tone morning summary paragraph** — implemented in `morning_system.md`
- [x] **Fix 8 — Clickable source links in tiered detail** — one best source per story, priority hierarchy in scoring rules
- [x] **Fix 9 — arxiv items tiered + labeled unreviewed** — implemented in scoring rules
